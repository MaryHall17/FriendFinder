var express = require("express");

var bodyParser = require("body-parser");

var path = require("path");

console.log("Express is linked: " + express);

console.log("Body parser is linked: " + bodyParser);

console.log("Path is linked: " + path);

